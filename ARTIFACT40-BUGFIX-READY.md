Artifact 40 bugfix branch build trigger.

The branch contains source-inspected fixes for returning-player join state, command blocking, FFA combat logging, FFA death drops, and the LobbyManager API mismatch.