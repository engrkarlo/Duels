name: Legacy UltimateDuels patch helper

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy auto-patching is disabled. Use the Paper 26.2 fix branch workflow instead."